# Test Cases

- Verify the UI loads correctly
- Confirm prediction values update for each input change
- Validate chart rendering and scenario buttons
- Ensure the repository structure is organized and accessible
