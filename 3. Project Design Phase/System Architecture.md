# System Architecture

The system follows a lightweight client-side architecture. The interface collects user input, JavaScript processes the prediction, and the application updates the results and chart instantly.
